/**
 * Public habitat — minimal shell per scaffold success criteria.
 * Only approved_for_publication + published artifacts should appear once wired.
 */
export default function PublicHome() {
  return (
    <main style={{ minHeight: "60vh", display: "flex", alignItems: "center", justifyContent: "center" }}>
      <h1 style={{ fontSize: "2rem" }}>Hello Twin!</h1>
    </main>
  );
}
